// Prisma schema for Maths4U Arena.
// Add models here when the app domain is ready.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}
